test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>
  
  
  amazon:
  service: S3
  access_key_id: <%= Rails.application.credentials.dig(:aws, 'AKIAQGDERNDHQK5SYS6Z') %>
  secret_access_key: <%= Rails.application.credentials.dig(:aws, 'nvGv2CJoaUTxsS4g6DIAvWdmuMMo/pZL5M5xMS+/') %>
  region: <%= Rails.application.credentials.dig(:aws, :s3, 'us-east-1') %>
  bucket: <%= Rails.application.credentials.dig(:aws, :s3, 'shota0523') %>